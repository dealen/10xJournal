{
  "Supabase": {
    "Url": "${SUPABASE_URL}",
    "AnonKey": "${SUPABASE_ANON_KEY}",
    "TestUrl": "${SUPABASE_TEST_URL}",
    "TestKey": "${SUPABASE_TEST_KEY}"
  },
  "TestUser": {
    "Email": "${TEST_USER_EMAIL}",
    "Password": "${TEST_USER_PASSWORD}"
  }
}