@using Microsoft.AspNetCore.Components
@using Microsoft.AspNetCore.Components.Forms
@using _10xJournal.Client.Infrastructure.ErrorHandling
@using System.Text.Json
@inject LoginHandler LoginHandler
@inject ILogger<LoginForm> Logger

<EditForm Model="loginModel" OnValidSubmit="HandleValidSubmit" aria-busy="@isBusy">
    <DataAnnotationsValidator />
    <ValidationSummary />

    @if (!string.IsNullOrWhiteSpace(errorMessage))
    {
        <p role="alert">@errorMessage</p>
    }

    @if (isBusy)
    {
        <p aria-live="polite">Logowanie w toku...</p>
    }

    <label for="login-email">Adres e-mail</label>
    <InputText id="login-email" @bind-Value="loginModel.Email" type="email" autocomplete="email" required />
    <ValidationMessage For="() => loginModel.Email" />

    <label for="login-password">Hasło</label>
    <InputText id="login-password" @bind-Value="loginModel.Password" type="password" autocomplete="current-password" required />
    <ValidationMessage For="() => loginModel.Password" />

    <button type="submit" disabled="@isBusy" aria-disabled="@isBusy">
        @(isBusy ? "Logowanie..." : "Zaloguj się")
    </button>

    <a href="/reset-password">Nie pamiętam hasła</a>
</EditForm>

@code {
    private readonly LoginModel loginModel = new();
    private bool isBusy;
    private string? errorMessage;

    [Parameter]
    public EventCallback OnLoginSuccess { get; set; }

    private async Task HandleValidSubmit()
    {
        if (isBusy)
        {
            return;
        }

        isBusy = true;
        errorMessage = null;

        try
        {
            await LoginHandler.LoginAsync(loginModel.Email, loginModel.Password);

            if (OnLoginSuccess.HasDelegate)
            {
                await OnLoginSuccess.InvokeAsync();
            }
        }
        catch (Supabase.Gotrue.Exceptions.GotrueException ex)
        {
            // Log the EXACT exception message for debugging mobile issues
            Logger.LogWarning(ex, "GotrueException during login for {Email}. Message: '{Message}', StatusCode: {StatusCode}", 
                loginModel.Email, ex.Message ?? "null", ex.StatusCode);
            errorMessage = AuthErrorMapper.MapLoginError(ex);
        }
        catch (Exception ex)
        {
            // Log detailed information about non-auth exceptions
            Logger.LogError(ex, "Unexpected error during login. Type: {ExceptionType}, Message: '{Message}'", 
                ex.GetType().Name, ex.Message ?? "null");
            errorMessage = "Wystąpił błąd połączenia. Spróbuj ponownie później.";
        }
        finally
        {
            isBusy = false;
        }
    }
}
